# Handoff — RC DC Standard prep, end of 2026-05-09 session

## TL;DR for picking this up

User (Jermey) is locking a Standard deck for **RC DC May 11-12 (2-3 days out)**. Three viable candidates:

| Deck | Sim FW WR | Real-world signal |
|---|---|---|
| **Hybrid (UB Bounce + Jermey)** | 73.2% | User getting cooked on Arena BO3 ranked |
| **HN Zevin v2 (UW Flash)** | 68.4% | No personal data |
| **HN "Lacco" top-20 build** | 67.2% (sim says worse) | 29-7 (81%) on ladder per screenshot |

User is "pretty convinced on UW Flash" but the specific build choice is open. **Real-world data > sim — user's own ladder pain with Hybrid is signal we can't ignore.**

The immediate pending decision when this handoff was written: **how to scrape Untapped.gg for Lacco's matchup data + replay logs.** Two paths offered, user hasn't chosen:
- **Path A**: User pastes their `sessionid` cookie from untapped.gg, I curl the API
- **Path B**: Install Playwright (~150MB) for headless browser scraping

---

## Today's session arc (commits)

### Morning: Counterspell engine wiring (commit `758e4b8`)
- Fixed `engine/counter_resolver.py` mana-gate bug. Lands never untap in `match_runner` so `_tap_lands_for_response` filtered all of them out → reactive pool always 0 → resolver never fired despite being wired.
- Post-fix conversion: 85% of in-hand-and-valid windows fire (was ~20%).
- **WR delta: +0.1pp at N=500.** "Tempo decks under-rate 5-10pp" hypothesis FALSIFIED. Memorialized in `memory/session_2026-05-08_counterspells.md`.

### Mid-day: HN pilot dialogue (commit `39bd315`)
- Encoded user-stated rules in `apl/azorius_high_noon_standard_match.py`:
  - High Noon T2 ASAP (removed prior is_control gating)
  - Aven Interrupter turn-skip targeting (exile opp's only castable nonland)
  - Avatar's Wrath threshold lowers to 2 creatures vs TRUE aggro (gruul/boros/monored — NOT landfall decks)
  - Floodpits stays flash-only vs Gruul ("at all costs")
- WR delta: -0.3pp (within noise). Pilot rules directionally correct but sim doesn't move.

### Afternoon: Hybrid handler audit + fixes (commit `85c7ec6`)
- User pushed back on Dream Beavers test (-7pp). Audit found:
  - **Stormchaser's Talent**: Level 2/3 effects never fired. APL set custom `_class_level` attribute; the `engine/classes.py` infrastructure reads `card.counters`. **Fixed**: APL now calls `engine.classes.level_up()` which does both.
  - **Spyglass Siren**: Auto +1 card on every ETB (over-modeling Map). **Fixed**: +1 card only on T3+ ETBs (more accurate Map sac timing).
  - **Cecil, Dark Knight**: Darkness drawback never modeled. **Fixed**: in `match_runner._run_player_turn`, lose 2 life per surviving Cecil that attacked.
  - **Dream Beavers**: Scry 1 missing. **Fixed**: 50% chance +1 card via deterministic turn parity.
- Re-baselined Hybrid: **73.2% FW** (was inflated 76.5%).
- Beavers vs Spyglass post-fix: **-0.9pp** (was -7pp). Equivalent — pick by feel.

### Late: UW Flash investigation
- User shared screenshot of top-20 ladder player **Lacco**, 29-7 (81%) UW Flash on Standard Bo3 Ladder (5/8/2026).
- Built `decks/azorius_high_noon_top20_standard.txt` (60+15).
- Tested vs 15-archetype field: **-1.1pp vs Zevin v2** (67.2% vs 68.4% FW).
- Diagnosis: top-20 list trades Get Lost (4 main) for Erode (2 main) + Wan Shi Tong (1) + 2nd Beza (1). Erode is {W} 1-mana removal but ramps opp a basic — bad vs ramp-heavy field. **List is metagamed for ladder (lots of Lessons/Control), worse for RC's projected ramp meta.**

---

## Outstanding work (NOT done)

### Engine handler audit (8 cards still need fixes)

Audit script: `scripts/audit_hybrid_handlers.py`. Output: `data/hybrid_audit.txt`.

User said "fix all wtf" — I started with the 3 high-impact ones (above), then ran out of session. Remaining:

| Card | Missing | Impact |
|---|---|---|
| **Quantum Riddler** | "If hand ≤1, draw +1 extra" static | Medium (under-rates in long games) |
| **Cecil, Dark Knight** | Transform to 4/4 lifelink Paladin (when life ≤ half) | Medium |
| **Day of Black Sun** | X hardcoded to 3, should scale with available mana | Medium |
| **Kaito, Bane of Nightmares** | +1 emblem, 0 surveil/draw, -2 stun (only hexproof body modeled) | Medium-high |
| **Tishana's Tidebinder** | Strips keywords approximation; doesn't actually counter the trigger | Medium |
| **The Unagi of Kyoshi Island** | Body 156 chars (stub). Ward-Waterbend {4} + opp-2nd-card-draw-2 trigger missing | Low (rarely played) |
| **Soul-Guide Lantern** | Tap+sac for full GY exile, {1}+T+sac for draw — not wired | Low |
| **Floodpits Drowner** | {1}{U}+T shuffle ability — docstring says "not yet wired" | Low |

### Untapped.gg scraping (immediate pending decision)

User wants matchup data + replay analysis from Lacco's profile (the 81% top-20 player).

**URL**: `https://mtga.untapped.gg/profile/a2c748b6-ad35-4b7b-939f-0ff62bee01f7/RPXT3QXG5RAWTOSCFCWABAWBOE?timeFrame=last_two_sets&constructedType=ranked`

**What I learned probing**:
- WebFetch fails — page is JS-rendered (Next.js / similar)
- API base is `api.mtga.untapped.gg` (per HearthSim/untapped-dev-proxy GitHub)
- Anonymous API access: `403 Missing Authentication Token` (despite proxy README claiming anonymous works)
- Replay URL pattern: `/replay/<replay_id>?gameStateId=N` — N increments per turn (each game state has its own URL)

**Two paths offered to user (no choice yet)**:
- **Path A**: User logs into untapped.gg → F12 → grab `sessionid` cookie → paste to me → curl API directly
- **Path B**: Install Playwright (~150MB) → render JS in headless Chromium → scrape DOM

User asked "maybe its easier if you looked first" — I did. Then asked for this handoff before deciding.

### SB guide (untrustworthy — DON'T recommend without testing)

Wrote `harness/knowledge/mtg/hybrid-jermey-sb-guide-rc-dc.md` for Hybrid early in session. User pushed back and asked for Bo3 validation. Tested via `scripts/test_hybrid_sb_guide.py`: **average -2.5pp Bo3**. Targeted matchups (Gruul -5.5, HN -5.0) got worse. Memorialized in `memory/feedback_sb_guide_validation.md`.

**Don't trust this SB guide. Don't write a new one without Bo3 validating first.** Real SB plans for RC should come from hand-tuned pilot guides, not sim card-text intuition.

---

## Critical user feedback to respect (from this session)

These are corrections or new feedback memories saved this session:

1. **Compare candidate decks by FW WR vs field, NOT head-to-head between candidates.** I made this mistake recommending Hybrid over Dimir base based on H2H. User: "we shouldnt be compairing these decks as if they awere playeg one another instead we should be gathering i should pilot one instead of the other". Memory: `feedback_deck_pick_framing.md`.

2. **SB guides need Bo3 validation, not card-text intuition.** Memory: `feedback_sb_guide_validation.md`. The current Hybrid SB guide was tested and is sim-negative. Future SB recommendations require running `scripts/test_hybrid_sb_guide.py` (or equivalent) FIRST.

3. **Always look up card oracle text BEFORE making claims.** From earlier session — user has tested me on this. The Untapped.gg attempt to "scrape" was driven by my own guess that data was accessible — should've looked first (which the user explicitly told me to do).

4. **Real-world pilot data overrides sim.** User said the 24-land variant feels better despite sim saying -0.9pp. The sim doesn't model variance (mana flood/screw distribution) or tap-land tempo cost. Trust pilot reports of "this feels bad" over sim's expected-value averaging.

---

## Files / artifacts created this session

### Code (committed)
- `engine/counter_resolver.py` — counterspell resolver (NEW)
- `engine/game_state.py` — counter window in `cast_spell()`
- `engine/match_runner.py` — wire `_match_opp_apl` on views; Cecil Darkness drawback
- `engine/card_handlers_verified.py` — Spyglass Map delay, Dream Beavers scry-1 proxy
- `apl/azorius_high_noon_standard_match.py` — pilot rules
- `apl/dimir_midrange_jermey_match.py` — Stormchaser level_up() integration

### Decks (committed)
- `decks/azorius_high_noon_top20_standard.txt` — Lacco's top-20 list

### Test scripts (all committed)
- `scripts/smoke_counterspells.py` — counter resolver smoke test
- `scripts/ab_counterspells.py` — counter resolver A/B WR
- `scripts/audit_hybrid_handlers.py` — handler oracle vs implementation audit
- `scripts/test_hybrid_sb_guide.py` — Bo3 SB plan validation
- `scripts/test_hybrid_24lands.py` — 23L vs 24L test (4 land variants)
- `scripts/test_hybrid_floodmaw.py` — Boomerang vs Into the Flood Maw
- `scripts/test_hybrid_dreambeavers.py` — Spyglass vs Dream Beavers
- `scripts/test_hybrid_manabase.py` — 24L mana base cleanup
- `scripts/test_hn_top20.py` — Lacco list vs Zevin v2

### Knowledge / memory
- `memory/session_2026-05-08_counterspells.md`
- `memory/session_2026-05-08_hn_pilot_and_pick.md`
- `memory/feedback_deck_pick_framing.md`
- `memory/feedback_sb_guide_validation.md`
- `harness/knowledge/mtg/hybrid-jermey-sb-guide-rc-dc.md` (UNTRUSTWORTHY — see above)

### Data files (uncommitted, local only)
- `data/ab_counters_*.txt` — counter A/B raw outputs
- `data/rc_hn_post_*.txt` — HN gauntlet outputs
- `data/rc_dimir_post_counter_fix.txt` — Dimir gauntlet output
- `data/hybrid_audit.txt` — full handler audit dump

---

## Concrete next actions for chat-Claude

1. **Decide Untapped.gg scrape path** with user. Path A (session cookie) is faster. If they pick it, curl pattern:
   ```bash
   curl -H "Cookie: sessionid=<USER_PASTES>" \
        -H "Referer: https://mtga.untapped.gg/" \
        -H "User-Agent: Mozilla/5.0 ..." \
        https://api.mtga.untapped.gg/api/v1/profile/a2c748b6-ad35-4b7b-939f-0ff62bee01f7/RPXT3QXG5RAWTOSCFCWABAWBOE
   ```
   Probe paths: `/api/v1/decks/<id>`, `/api/v1/replay/<id>`, `/api/v1/replay/<id>/states`. The `gameStateId=N` URL params suggest there's a per-state endpoint.

2. **If Untapped data lands**: build matchup table for Lacco's HN. Compare vs sim's predicted matchup spread. Identify cards/lines the sim doesn't capture.

3. **Lock the deck**: Hybrid (real-world cooking user, sim 73.2%) vs HN Zevin v2 (untested by user, sim 68.4%) vs HN top-20 list (Lacco's, sim 67.2% but real 81%). User leaning UW Flash. Decision should be based on what user can pilot well in 2-3 days — pilot familiarity > sim FW gap of <5pp.

4. **If time permits**: finish handler audit fixes for Quantum Riddler empty-hand bonus, Cecil transform, Day of Black Sun X scaling, Kaito abilities. Each ~10-20 min. Re-baseline after.

5. **DO NOT** write a new SB guide without Bo3 validation. The script exists at `scripts/test_hybrid_sb_guide.py`.

---

## How to verify state

```bash
cd "E:/vscode ai project/mtg-sim"
git log --oneline -10                                # recent commits
git status                                          # uncommitted (yesterday's work + data files)
python scripts/test_hn_top20.py                     # rerun Lacco vs Zevin
python scripts/test_hybrid_dreambeavers.py          # rerun Beavers test
cat memory/MEMORY.md                                # full memory index
```

Today is **2026-05-09**. RC DC is **2026-05-11 / 2026-05-12** (2-3 days out).
